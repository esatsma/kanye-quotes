// import { expect, test, vi } from "vitest";
// import { render } from "@testing-library/react";
// import QuoteBlock from "../components/templates/QuoteBlock/Quoteblock";

// test("Displays an image of Kanye", async () => {
//   vi.mock("../components/QuoteCard/QuoteCard", () => (props) => {
//     mockChildComponent(props);
//     return <mock-quiteComponent />;
//   });

//   const quoteBlock = render(<QuoteBlock />);

//   expect(mockQuoteComponent).toHaveBeenCalledWith(
//     expect.objectContaining({
//       quote: "email",
//     })
//   );

//   quoteBlock.unmount();
// });
